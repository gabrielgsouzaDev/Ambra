/** Taxa fixa por recarga (paga pelo responsável). Em centavos: R$ 1,99. */
export const RECHARGE_FEE_CENTS = 199;

/** Recarga mínima aceita (R$ 5,00) — evita cobrança irrisória. */
export const MIN_RECHARGE_CENTS = 500;

/** Valores sugeridos na UI (R$ 50 / 100 / 150) — incentivam recarga maior. */
export const SUGGESTED_AMOUNTS_CENTS = [5000, 10000, 15000];
